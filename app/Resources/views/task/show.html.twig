{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/main.css') }}"/>
{% endblock %}

{% block title %}Show Task{% endblock %}

{% block body %}
    <ul>
        <li>{{ task.name }}</li>
        <li>{{ task.dataCriacao | date('d/m/Y') }} às {{ task.dataCriacao | date('H:i:s') }} </li>
        <li>{{ task.status }}</li>
    </ul>
{% endblock %}